{
  "version": "0.2.0",
  "configurations": [
    
    {
      "name": "Run example",
      "type": "python",
      "request": "launch",
      "program": "${workspaceFolder}/run_example.py",
      "console": "integratedTerminal",
      "env": {
        "PYTHONPATH": "${workspaceFolder}"
      }
    }
  ]
}